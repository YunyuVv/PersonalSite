# 2026-07-25

## 接入 shadcn/ui + Magic UI 到 PersonalSite
- 安装依赖：`next-themes`、`class-variance-authority`、`clsx`、`tailwind-merge`、`@radix-ui/react-slot`、`@radix-ui/react-navigation-menu`。
- 手动完成 shadcn "init"（绕过交互式 CLI，避免覆盖手写 globals.css）：新增 `components.json`；把 `src/lib/utils.ts` 的 `cn` 升级为 `clsx + tailwind-merge` 标准版。
- 在 `src/app/globals.css` 注入 shadcn 设计 token：在 `@theme inline` 映射 `--color-primary/secondary/card/popover/muted/accent/destructive/border/input/ring` 到现有 `--accent/--bg-muted/--divider/--text-*` 等变量；新增 `--destructive` 红到 `:root`/`.dark`。**未重定义 `--accent` 变量名**（现有大量代码用原始 `var(--accent)` 作品牌蓝），shadcn 的 `--color-accent` 直接映射到 `var(--bg-muted)`。
- 主题迁移：`use-theme.ts`（自定义 hook）删除，统一改用 `next-themes`。新增 `src/components/theme-provider.tsx`（`attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange`），`layout.tsx` 包裹；`theme-toggle.tsx` 与 `nav.tsx` 改为 `next-themes` 的 `useTheme` + `mounted` 守卫（避免水合不一致）。与原有 `.dark` 类 + localStorage `theme` 键完全兼容。
- 新增 shadcn 基础组件：`button.tsx`、`card.tsx`、`badge.tsx`、`navigation-menu.tsx`（new-york 风格，标准实现）。
- Magic UI `ShimmerButton`：`src/components/magic-ui/shimmer-button.tsx`，扫光用 `::before` 伪元素（不在按钮内嵌 `<style>`，避免非法 HTML），支持 `asChild`（Radix Slot）包 `<Link>`。已接入首页 Hero 主 CTA「查看简历」（替换原 `hm-btn-primary`）。
- 构建：本地 `NODE_OPTIONS= pnpm run build:worker` 通过，全部路由静态预渲染，`/open-next/worker.js` 生成。
- 修复两处构建阻断：1) `nav.tsx` 残留 `@/hooks/use-theme` 引用（改为 next-themes）；2) `src/app/map/page.tsx`（之前"自架构图"原型，从未跑过 `next build`）的 TS 错误：`SkillBars` 把技能分组数组当单组用 `cats.items` → 改为 `cat.items`；节点 `na.x/na.y` → `na.pos.x/na.pos.y`。map 页是未确认的创意原型，先修好让部署不被阻断。

## 待办 / 注意
- shadcn 组件已就位但尚未在主站页面大量替换旧手写组件（仅 Hero CTA 用了 ShimmerButton）。Button/Card/Badge/NavigationMenu 可作为后续规范化基础。
- 用户此前否掉"炫光酷炫"和"像素风"两种主页风格；shadcn/Magic UI 是通用组件框架，不是风格，已安全接入。
