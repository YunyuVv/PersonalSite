# 2026-08-29

## PersonalSite-home：admin 登录鉴权落地（阶段 B）

已完成「登录页 + Cookie 会话 + 后端写 GitHub」完整闭环，详见 `docs/cloudfalre部署/阶段B-后端接口与Admin方案.md`（已更新到最新实现）。

要点：
- 新增 `src/lib/server-env.ts`（统一取 env）：Cloudflare ctx 优先，缺失项补 `process.env`（兼容本地 `next dev`）。
- 新增 `/api/admin/login` 下发 HttpOnly+SameSite=strict Cookie；`/api/admin/logout` 清 Cookie；`/admin` 服务端组件 force-dynamic，未登录 `redirect("/admin/login")`。
- `/api/admin/update` 同时接受 `x-admin-key` 头与 `admin_session` Cookie。
- 本地 `.env.local` 写入 `ADMIN_KEY=PersonalSiteBiliww`（已被 .gitignore 忽略）。线上用 `wrangler secret put ADMIN_KEY` / `GITHUB_TOKEN`。仓库 `YunyuVv/PersonalSite` @ `main`。
- 关键坑：`getCloudflareContext({async:true}).env` 在本地 dev 返回非空对象（含 ASSETS 等绑定），所以必须先判 ADMIN_KEY 是否存在、再从 process.env 补，否则 dev 下自定义变量读不到导致登录 401。已沉淀为 skill `opennext-cloudflare-env-auth`。

构建通过：`/` 静态（○），`/admin` 及三个 API 动态（ƒ），静态部署原则未破坏。

## admin 保存增加确认弹窗
- `src/components/admin/admin-form.tsx`：先使用 `window.confirm`，后替换为自定义 `ConfirmDialog` 组件（`src/components/ui/confirm-dialog.tsx`）：居中卡片 + 遮罩 + 淡入缩放动画，保存前二次确认，防止误触占用 Worker 每日部署额度。
- 构建通过；dev 仍运行于 12001，HMR 生效。

## 站点图标替换为 YunYu Logo
- 用 `/Users/wangpenglong/Downloads/yunyu黑色压缩.webp`（黑字白底版）生成：
  - `src/app/favicon.ico`：16/32/48/64 多尺寸 ICO（RGBA，适配 Next.js Turbopack）。
  - `src/app/icon.png`：192×192 站点图标。
  - `src/app/apple-icon.png`：180×180 Apple touch icon。
- 全部做圆角白底（四角透明，半径比 0.22）内嵌，完整保留 "YunYu" 文字；黑色版本解决了上一版白字白底看不清的问题，圆角解决方块锐角生硬问题。
- 图标从 `src/app/` 约定式特殊文件**迁移到 `public/`**（`public/icon.png`、`public/apple-icon.png`、`public/favicon.ico`），`layout.tsx` 的 `icons` 元数据已用根路径 `/icon.png` 等引用，无重复 link。

## admin 页增加「返回首页」按钮
- `src/components/admin/admin-form.tsx` 页头右侧按钮组（返回首页 / 主题切换 / 退出登录）新增 `Link href="/"`「返回首页」。
- `src/app/admin/login/page.tsx` 右上角（返回首页 / 主题切换）同样新增「返回首页」链接，保持 admin 流程一致。
- 构建通过。

## MBTI 区块拆分为独立 MBTICard 组件并美化
- 新建 `src/components/sections/mbti-card.tsx`：
  - 圆角卡片 + 渐变光晕背景 +  subtle backdrop-blur；
  - 左侧人物插画加入缓慢上下浮动动画；
  - 右侧整合类型标题/描述、维度分析子面板、核心优势/潜在盲区标签；
  - 维度条重做：两端字母+名称高亮主导侧，渐变填充条从主导侧生长，底部显示「主导：X 名称」与百分比。
- `src/components/sections/mbti-section.tsx` 的 hero 变体改为直接渲染 `<MBTICard />`，清理已废弃的 STAGGER/ITEM_FADE。
- bento 变体保留原实现，避免影响简历页。
- 构建通过。
- 后续调整：用户反馈卡片背景突兀，已去掉外层卡片背景与人物插画底部光晕，维度分析面板改为仅顶部细线分隔；保留左上角 accent 光晕，并把右侧内容整体缩小一号（标题、描述、维度条间距与标签间距），让左右视觉更平衡。
- `src/app/layout.tsx` 显式声明 `icons: { icon, shortcut, apple }`。
- 构建通过：`/`、`/icon.png`、`/apple-icon.png` 均为静态预渲染。

## admin 后台接入统一主题（与首页共用一套）
- 根因：ThemeProvider(next-themes) 已在 layout.tsx 包裹全部页面，主题状态存同一 localStorage 键；但 admin 页面用的是硬编码 `neutral-*`/`bg-white`，不随 `.dark` 切换。
- 改动：把 `admin-form.tsx`、`admin/login/page.tsx`、`confirm-dialog.tsx` 的硬编码浅色类全部替换为语义 CSS 变量（`bg-[var(--bg-primary)]`、`bg-[var(--bg-card)]`、`border-[var(--divider)]`、`text-[var(--text-primary)]`、`text-[var(--text-secondary)]`、`bg-[var(--accent)]`、`text-[var(--text-on-accent)]` 等），自动跟随明亮/暗黑/系统。
- 在 admin 页头与登录页右上角接入 `ThemeToggle`，可直接切换主题。
- 构建通过；dev 仍运行于 12001，HMR 生效。
