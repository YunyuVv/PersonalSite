# 2026-09-03

## ThreeUI 组件选型与页面设计（docs/前端ui/threeui/ThreeUI组件选型与页面设计.md）
- 调研 ThreeUI（MengTo/DesignCode）Community 开源版：102 个组件，MIT，仅 peer react/react-dom/three（无需 R3F/drei）。项目已满足（react19.2.4 + three0.185）。
- 关键风险：包内 three128/three165 别名会多装两份 three；部分场景/画廊类组件需复制 lib-dist/assets 到 public 或传 assetBaseUrl。
- 设计为新路由 `/threeui` 作 3D 视觉实验室。评估矩阵分 S/A/B：S=GradientCta/GlassmorphismCta/SpinningBorderButton/LumenCta/GradientPillButton；A=ConstellationField/OrbitalSphereBackground/NeonTypography/ParticleWordmark/AnimatedTopDock/EditorialIntroSection/NewsletterFooterSection 等自包含组件；B=场景类（需资产验证）。
- 约定沿用 reactbits 模式：ThreeUIClient 包装（'use client' + mounted 守卫 + useReducedMotion 降级）。下一步待用户确认是否直接落地 /threeui 页面代码。

## /threeui 页面落地（v1，已 build 通过）
- 用户确认：组件落 `src/components/threeui/`，直接落地 `/threeui` 页面一版。
- 实测修正：ThreeUI 效果类组件（ConstellationField/NeonTypography/GradientCta 等）props 仅 mode/hue/saturation/brightness/className/style，**无 text/children**，页面定位改为「组件橱窗」，真实文案用自有 h1。
- 实现：`src/components/threeui/threeui-client.tsx`（ThreeUIClient 包装 + useThreeUIMode + 10 个 dynamic(ssr:false) 子路径再导出）+ `src/app/threeui/page.tsx`（ConstellationField 背景 + Hero + 文字卡 + 7 按钮演示）。
- `pnpm add @designcodeio/threeui@1.2.0`（peer 满足，无需 R3F）。`pnpm build` 通过（Next16 Turbopack，/threeui 静态预渲染）。未起 dev server（用户偏好静态校验即可）。
- 待观察：组件内置按钮标签是否外链 threeui.com；three128/three165 额外体积；B 级场景/画廊需复制 assets。

## /home-threeui hero 页 + /threeui 更名 /demo-threeui（v2，已 build 通过）
- 用户要求：用 Sylva hero（threeui.com/hero/sylva）做 `home-threeui` 页面的 hero 介绍自己；原 `/threeui` 页更名 `/demo-threeui`。
- 实测 SylvaHero：落地页型组件，通过 `<iframe src="/landing-pages/inner-green-3d.html">` 加载打包 HTML，文案刻进 HTML（"Sylva — Into the living world"），**无 props 改姓名/简介**；props 仅 className/srcDoc/style + 排版项（primaryColor/headingFont/headingSize…）。
- 落地：复制包内 `lib-dist/assets/landing-pages/inner-green-3d.html` + `inner-green-assets/` → `public/landing-pages/`（iframe 硬编码相对路径，相对引用已校验全部存在）；`threeui-client.tsx` 新增 `SylvaHero` dynamic(ssr:false) 再导出；`src/app/home-threeui/page.tsx` 以 SylvaHero 全屏背景 + 自有姓名/角色/简介/CTA 叠加层介绍自己；`src/app/threeui` → `src/app/demo-threeui`（git mv 失败走 mv fallback），demo 页顶栏新增「ThreeUI Hero ↗」互访链接。
- `pnpm build` 通过（/demo-threeui、/home-threeui 均静态预渲染）。未起 dev server。
- 待观察：绿色 living-world 场景与站点蓝强调色是否需 primaryColor 收敛（当前保留默认绿调保真）。

## /home-threeui 清理 Sylva 模板 UI（v3，已 build 通过）
- 用户反馈截图：Sylva 模板自己的导航/标题/卡片/按钮与自有内容重叠。
- 根因：SylvaHero iframe 加载完整落地页 HTML，模板 UI 写死，无 props 关闭。
- 解决：修改 `public/landing-pages/inner-green-3d.html`，在 `<head>` 注入 CSS 隐藏 `.dock-wrap/.ghost/.card/.headline/.lede/.pill-clip/.play-wrap/.stat/.knob-float/.guides`，只保留 `<canvas id="scene">` 3D 场景。
- 同步：home-threeui 页 hero 叠加层改为居中（`items-center text-center`、`justify-center` 按钮），在干净背景下更平衡。
- `pnpm build` 通过。刷新浏览器可看到模板 UI 消失，只剩 3D 背景 + 姓名/简介/CTA。
- 注意：该 HTML 已是定制资产，升级 threeui 包后需重新复制并保留隐藏 CSS；DOM 结构变化时需更新 selector。

## /home-threeui 改用 Sylva 场景组件 + 文字/按钮组件构建 hero（v4，已 build 通过）
- 用户要求：① 删除 v3 复制的完整 HTML（public/landing-pages/）；② 引入 threeui.com/three-js/sylva-living-world 这个**场景专用**组件；③ 在其上用 ThreeUI「其他组件」构建符合本项目的 hero。
- 删除：`rm -rf public/landing-pages`（inner-green-3d.html + inner-green-assets 一并移除）；`threeui-client.tsx` 移除 SylvaHero 导出。
- 关键发现：包内存在**可自定义文字**且**无硬编码外链**的组件 —— `SemanticBloom`(text?，默认"Codex"，mode/size/opacity)、`LiquidMetalButton`(text? + onClick，variant pill/circle/play)、`LumenCta`(label? + onClick，variant primary/ghost，mode light/dark)。其余效果类（ConstellationField/NeonTypography 等）仍只有 mode/hue/saturation/brightness，无 text。
- 引入 `@designcodeio/threeui/components/SylvaLivingWorldScene`：**纯 Three.js 场景，模板 UI 已移除**，variant 当前仅 "living-green"，props 仅 variant/className/style，**无需 iframe、无需复制打包 HTML、无硬编码文案**。子路径 require.resolve 校验 OK。
- 重写 `src/app/home-threeui/page.tsx`：SylvaLivingWorldScene(variant="living-green") 作全屏背景（ThreeUIClient 守卫，降级绿色径向渐变）+ SemanticBloom(text=profile.name, mode="dark", size=1) 作姓名（sr-only h1 兜底无障碍）+ 自有 role·location/tagline/intro（framer-motion）+ LiquidMetalButton("查看作品"→/creative) + LumenCta("联系我"→mailto) + 自有"简历" Link。
- `threeui-client.tsx`：SylvaHero→SylvaLivingWorldScene，新增 SemanticBloom 导出，LiquidMetalButton/LumenCta 沿用原有导出（去重，避免重复声明）。
- `pnpm build` 通过（/home-threeui、/demo-threeui 静态预渲染）。未起 dev server。
- 待用户浏览器确认：SemanticBloom mode（暗绿背景上 "dark" 应为浅色发光字，否则改 "light"）；size 默认 1 长名字可下探 0.7–0.8；LumenCta mode="light" 对比；three128/three165 额外体积。

## /home-threeui 回退 SylvaHero iframe + 完整定制落地页文案（v5，已 build 通过）
- 用户反馈：v4 的 `SylvaLivingWorldScene` 偏暗、不够舒畅，且叠加内容挡住了背景；更喜欢 iframe 完整落地页的明亮色调、鼠标悬停树枝交互。
- 决策：回退到 `SylvaHero` iframe，**不再叠加自有 DOM**，而是直接修改 iframe 内 HTML 的默认文案为个人内容，保留全部原生交互与明亮视觉。
- 恢复：`mkdir -p public/landing-pages && cp inner-green-3d.html + inner-green-assets/` 从包内重新复制；新增 `scripts/customize-sylva.py`，用替换表把 Sylva 默认文案改为 YunYu 内容。
- 文案替换：标题/描述、headline(YunYu/全栈工程师)、lede、dock(首页/作品/笔记/联系)、ghost(YUNYU)、卡片(关于我/以代码写诗…；精选项目/微前端治理平台)、统计(Web 开发经验/5 年+；主要技术栈/React / Next.js)、按钮(查看作品/关于我)、滚动提示(向下探索)。
- 跳转：iframe 内链接用 `target="_top"`，按钮用 `onclick="top.location.href='...'"`；同域可用。
- `src/components/threeui/threeui-client.tsx` 恢复 `SylvaHero` 导出；`src/app/home-threeui/page.tsx` 简化为只渲染全屏 `SylvaHero`。
- `pnpm build` 通过（/home-threeui、/demo-threeui 静态预渲染）。未起 dev server。
- 维护提示：升级 threeui 后需重新复制 HTML 资产并重新运行 customize-sylva.py；中文用系统字体 fallback，如需统一可注入中文字体 CSS。

## /home-threeui 拆分官方包导入 + 当前实现备份（v6，已 build 通过）
- 用户看到官方 skill 示例 `import { SylvaHero } from "@designcodeio/threeui"` + style.css + 排版 props，要求：① 当前 v5 实现开新页面保留为 back；② /home-threeui 改用官方包导入。
- 实测核实：SylvaHero 确实从包根导出、style.css 存在（"直接 import 即可"成立）；但 v1.2.0 真实 props 为 PageTypographyProps（headingFont/bodyFont/headingWeight/bodyWeight/primaryColor/headingSize/bodySize/headingLetterSpacing），**无 variant 字段**（skill 里 variant 属另一构建）；排版经 postMessage 注入同源 iframe，仍需 public/landing-pages HTML 资产，headline 文字仍刻在 HTML（改名字需定制）。
- 落地：`src/app/home-threeui-back/page.tsx`（v5 备份，threeui-client 包装）+ `src/app/home-threeui/page.tsx`（dynamic import 包 components/SylvaHero + style.css + 排版 props，不传 variant/primaryColor）；两页面共用定制 HTML 资产。
- `pnpm build` 通过（/home-threeui、/home-threeui-back、/demo-threeui 静态预渲染）。未起 dev server。

## /home-threeui 加 primaryColor 调蓝色调（v6 微调，已 build 通过 → 已回滚）
- 用户在 /home-threeui 的 SylvaHero 加 `primaryColor="#5b8def"`（站点亮色强调色），把 hero 文字/两枚原生控件 tint 收敛成蓝色调。
- 实测认知：primaryColor 仅改 hero ink 与两枚 liquid-metal 控件派生色，**3D 场景本体（绿色）不变**。若要把整个场景变蓝需换 variant（community 版无）/加滤镜，非 primaryColor 能力范围。
- 用户随后要求「色彩回滚回去」，已移除 primaryColor，恢复 Sylva 原版明亮绿调（/home-threeui 与 /home-threeui-back 一致）。`pnpm build` 通过。未起 dev server。

## /home-threeui 首屏禁止滚动（v6 微调）
- 用户反馈截图：首屏出现滚动条，下面有 "向下探索" 与 card-stack 等区块。
- 解决：在 `public/landing-pages/inner-green-3d.html` 的 `<style>` 末尾注入 `html,body{overflow:hidden;height:100%}` + 隐藏 `.section` 与 `.scroll`，仅保留首屏 hero 内容。
- 同步更新 `scripts/customize-sylva.py`，确保升级 threeui 后重新定制文案时不会丢失该样式。
- `pnpm build` 通过。未起 dev server。
