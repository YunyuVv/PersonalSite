## 2026-07-24

- 创建个人站点需求文档 v1.0 (`docs/requirements.md`)：多页面架构 + 博客 + 项目详情
- 创建技术实现文档 v1.0 (`docs/technical-design.md`)：Next.js 15 + TypeScript + Tailwind CSS v4 + SSG/ISR + MDX

- **重构 v2.0**：根据用户反馈，将站点重新定位为"国风数字简历"
  - 需求文档 (`docs/requirements.md`)：单页到底设计，5 个 Section（Hero/关于/技能/项目/联系），国风水墨美学（墨色/宣纸色/朱砂红/金色），详细动画规格（Canvas 墨点粒子、SVG 山脉视差、滚动驱动入场动画、framer-motion），目标受众聚焦 HR/面试官
  - 技术实现文档 (`docs/technical-design.md`)：简化架构为纯 SSG 单页，移除博客/MDX/多路由，新增 Canvas 粒子系统、SVG 视差、framer-motion useScroll 动画架构，数据模型简化为单一 `profile.ts`，国风装饰组件（Seal/BrushDivider/MountainParallax），性能约束针对动画密集型场景

- **项目开发完成**：
  - 技术栈：Next.js 16 + TypeScript + Tailwind CSS v4 + framer-motion + lucide-react
  - 组件架构：layout (Nav/Footer) + sections (Hero/About/Skills/Projects/Contact) + effects (InkParticles/MountainParallax) + ui (Seal/BrushDivider/SkillRadar/Timeline/ProjectCard/BackToTop) + hooks (useScrollSpy/useReducedMotion/useCopyToClipboard/useTheme)
  - Hero：Canvas 墨点粒子 + 4 层 SVG 山脉视差 + 云雾漂移 + 姓名逐字浮现 + tagline 打字机效果
  - About：个人自述 + Timeline 时间轴（描边动画 + 左右交替滑入）
  - Skills：SVG 技能雷达图（5 轴）+ 技能标签 hover 高亮联动
  - Projects：3 列卡片网格 + hover 遮罩外链 + stagger 入场动画
  - Contact：邮箱复制 + 社交链接 + 印章 Seal 落款
  - 深色/浅色模式切换 + 动画降级 (prefers-reduced-motion) + 滚动 spy 导航高亮
  - 构建通过 (next build 零报错)，dev server 运行在 localhost:3100

  - **深浅模式全面适配**：
    - globals.css 新增 `--hero-bg`/`--hero-text`/`--hero-text-muted`（Hero 区专用）和 `--overlay-bg`/`--overlay-border`/`--overlay-text`（深色区半透明覆盖层）和 `--text-on-accent`
    - hero-section.tsx：所有硬编码 hex (#e8e0d5/#a0a0a0/#121212) → CSS 变量
    - ink-particles.tsx：Canvas 粒子动态读取 `--text-primary` 并转 RGB，MutationObserver 监听主题切换实时更新、深色模式粒子不再消失
    - project-card.tsx：`bg-white`/`text-white` → `--text-on-accent` 变量
    - contact-section.tsx：`bg-white/10` 等 → `--overlay-bg`/`--overlay-border`/`--overlay-text`
    - section-wrapper.tsx：`dark` prop 硬编码颜色 → `--hero-bg`/`--hero-text`

- **frontend-design skill 审查 + 改进**（P0 + P1）：
  - P0-1: 砍掉 MountainParallax 中的 CloudDrift 云雾漂移组件；InkParticles count 150→40，减少动画噪音
  - P0-2: 全局字号提升：Hero姓名 5xl→6xl/7xl→8xl/8xl→9xl；Section标题 3xl→4xl/4xl→5xl；About正文 base→lg/lg→xl；Contact CTA lg→xl/xl→2xl；About子标题 xl→2xl
  - P1-1: globals.css 新增 `--lapis`/`--lapis-light`（青金石蓝 #2b5f8a），浅/深色双模式
  - P1-1: Nav 桌面+移动端 active pill `--accent`(朱砂红) → `--lapis`(青金石蓝)
  - P1-1: 技能雷达图数据 fill/stroke `--accent` → `--lapis`，朱砂红专用于印章/CTA/accent，冷暖功能色分离
  - 安装 Anthropic frontend-design skill 到 ~/.workbuddy/skills/frontend-design/

- **靛蓝色系重构**（替换朱砂红+金色+青金石蓝）：
  - globals.css 完全重写色彩体系：冷白底 `#f8f8f7`，单一靛蓝 accent `#4a5b8c`，冷灰线 `#d4d4d8`
  - `--gold`/`--lapis`/`--celadon` 全部别名到靛蓝值，零改动组件文件就完成全站换色
  - 深色模式：`#111113` 底 + `#6b7eb0` 靛蓝
  - 呼吸感：Section py 24→40/32→56，max-w 6xl→5xl 收窄20%

- **完全推翻国风，现代网站重设计**：
  - Hero：移除 MountainParallax/InkParticles/打字机效果 → 大号字体 + 几何圆形装饰 + 细分割线 + SCROLL 指示
  - About：左右两栏布局——左栏标题+自述，右栏竖线时间轴（带圆点）+ experience + education
  - Skills：完全重写为图标卡片网格（grid-cols-2/3/4），每项 skill 一个卡片含图标 + 名称 + 5 颗圆点熟练度。背景 `bg-muted` 区隔
  - Projects：3 列卡片网格，顶部色块+H1字母占位，底部 tags + demo/source 链接
  - Contact：极简居中，靛蓝边框胶囊按钮 + 社交图标圆形按钮。背景 `bg-muted` 区隔
  - Skills 和 Contact 背景用 `bg-[var(--bg-muted)]` 做视觉分区
  - 保留靛蓝色系，零国风元素，干净现代

- **卷轴展开技能展示**（需求文档方案三落地）：
  - 新建 `scroll-reveal.tsx`：古画卷轴组件（木轴 SVG + 宣纸纹理 + clip-path 展开动画 + 技能标签）
  - 结构：TopRoller（上木轴）→ 两侧挂绳 → 画布容器（clip-path: inset(0 0 100% 0) → inset(0 0 0% 0)）→ BottomRoller（下木轴）
  - 木轴用 SVG linearGradient 模拟木质质感（#c4a46c → #8b6b3c），轴头加粗
  - 画布内技能按类别分组，每个标签颜色深浅 = 熟练度（`color-mix(in srgb, var(--lapis) ...)`），右侧 5 个圆点指示等级
  - 标签 stagger 弹入动画（delay + 0.04s/个），hover 放大 1.1x
  - skills-section.tsx 替换 SkillRadar → ScrollReveal
  - 原有 skill-radar.tsx 保留未删除，可随时切回

- **苹果玻璃拟态 + 苹果蓝配色**：globals.css 改为 Apple 风（#f5f5f7 底 / #3b5ccc 苹果蓝 accent），引入 `.glass` 玻璃类、Inter 字体、AnimatedBackground 浮动光斑。accent 后调为 #3b5ccc（浅）/ #5b8def（深）。

- **Bento 网格全面重做（用户反馈"太垃圾"后选定路线 02 现代 Bento / 苹果风升级）**：
  - 设计参照：Brittany Chiang、rubenmarcus.dev、Framer Bento 模板（Hanssen/Villo）、Spatzek/Obys 创意派
  - 新增 `.bento-card`（圆角24px + 1px边 + 柔和阴影 + hover 抬升/描边 accent）、`.bento-label`（小号大写强调色标签）；`body::before` 极淡顶部径向光晕替代廉价浮动光斑
  - 新建 `src/components/ui/reveal.tsx`（framer-motion 滚动入场，支持 id 锚点）
  - page.tsx 改为 6 列 Bento 网格：Hero(col-span-4) + 侧栏(状态/数据/聚焦, col-span-2) + 关于(col-span-3) + 技能(col-span-3) + 经历(col-span-6) + 项目(col-span-6, 2列网格) + 联系(col-span-6)
  - 各 Section 重写为 Bento 卡片：Hero(大字号+求职徽章+双CTA)、About(自述分段)、Skills(分类 chip)、Experience(新建紧凑时间线列表)、Projects(ProjectCard 去厚重遮罩改 hover 浮出按钮)、Contact(整宽卡片+邮箱+社交)
  - 删除 AnimatedBackground 引用；Footer 去掉易报错的动画开关；Nav 容器宽度对齐 max-w-6xl
  - 构建零报错（注意：需 `env -u NODE_OPTIONS` 才能绕过环境里非法的 --use-system-ca NODE_OPTIONS）
  - dev server 运行于 localhost:3100（持久后台 task_id: wUQr60）

- **新增 /creative 创意互动风页面（frontend-design skill 路线 03 创意互动 / Spatzek 风）**：
  - 设计方案（用户选"你定，给我惊喜"）：深墨底 `#0B0B0F` + 电光紫 `#7C5CFF` / 珊瑚 `#FF5C7C` 双色渐变签名，**避开** AI 默认三套路（米色衬线+陶土、近黑+酸绿单色、报纸网格）
  - 字体：展示用 Bricolage Grotesque（怪诞体），标注用 Space Mono，正文 Inter（均经 `<link>` 在 creative 页内加载，不污染 globals）
  - 新建 `src/app/creative/creative.css`：独立设计系统（深墨令牌/自定义光标/氛围光球 keyframes/跑马灯/卷轴卡/磁吸标签），reduced-motion + 触屏降级；`cursor:none` 仅在 `(pointer:fine) and (prefers-reduced-motion:no-preference)` 时启用
  - 新建 `src/app/creative/page.tsx`（client）：4 个核心交互组件
    - `MagneticCursor`：mix-blend-mode:difference 双环光标，useSpring 跟随，hover a/button 时放大；仅在精确指针启用
    - `Magnetic`：按钮/链接磁吸包裹（mousemove 计算偏移 *0.35，离开归零）
    - `Reveal`：滚动入场（whileInView，reduced-motion 直出）
    - `ProjectReel`：framer useScroll 横向卷轴（桌面+非降级时 pinned 320vh + x 位移 -66%；移动/降级回退 overflow-x-auto 原生横滑）
  - Hero(22vw 超大姓名+跑马灯) / 关于宣言 / 项目横向卷轴 / 技术栈磁吸标签云 / 经历大行 / 联系巨型 CTA
  - 社交图标映射规避 lucide 缺失品牌图标：github→Code2, linkedin→Briefcase, juejin→BookOpen, zhihu→MessageCircle
  - `nav.tsx` 增加"互动版"链接（桌面 pill + 移动菜单项）指向 /creative，两版互跳
  - `pnpm build` 零报错，/creative 静态生成；dev server localhost:3100 验证 200 + 内容齐全

- **/creative 光标恢复正常**：用户反馈自定义磁吸双环光标"太怪"，移除 `MagneticCursor` 组件及调用、删除 `creative.css` 中 `.c-cursor` 样式与 `@media (pointer:fine) cursor:none` 规则，恢复系统原生光标。磁吸按钮/横向卷轴/跑马灯等交互保留（不依赖自定义光标）。

- **安装 ui-ux-pro-max skill（用户指定 `npx skills add ...`）**：
  - 来源仓库 `nextlevelbuilder/ui-ux-pro-max-skill`（main 分支），MIT 许可，nextlevelbuilder 维护
  - 安全审计结论 **P2（安全）**：核心 `scripts/search.py` 为纯本地 BM25 检索（仅 import 项目内 core/design_system + argparse/json/io），无网络调用/无 subprocess/无 rm -rf/无 eval-exec；`--persist` 仅写用户 `--output-dir` 下的 `design-system/<slug>/`，默认不覆盖已有 MASTER.md
  - 安装位置 `~/.workbuddy/skills/ui-ux-pro-max/`（43 文件：SKILL.md + data/ 14 CSV + references/ 2 md + scripts/ 5 py）
  - 实测可用：`python3 scripts/search.py "portfolio minimal dark" --domain style -n 1` 成功返回 OLED Dark 风格结果
  - 注意：SKILL.md 内脚本路径用 `${CLAUDE_PLUGIN_ROOT}` 占位符（Claude Code 变量），WorkBuddy 调用时改用绝对路径 `~/.workbuddy/skills/ui-ux-pro-max/scripts/search.py`

- **用 ui-ux-pro-max skill 重构需求/技术文档为 v3.0（多页架构）**：
  - 调用 skill 检索设计情报：首页→Soft UI Evolution（单色+蓝点缀 #18181B/#2563EB/#FAFAFA/#E4E4E7，浅深双模式）；字体 Archivo/Space Grotesk（首页 display）+ Inter（正文/简历）；动效 Stagger 标准 300-450ms back.out(1.4)；简历页沿用已实现 Bento 苹果风
  - 新信息架构：`/` 个人主页(门户) + `/resume` 简历子页(Bento 苹果风，从现有 / 迁过去) + `/creative` 互动版(保留)
  - 设计令牌统一：globals.css CSS 变量（--accent 苹果蓝 #2563EB/#5B8DEF，与现有 #3b5ccc 可二选一），首页/简历页共享
  - 响应式强制四档：375/768/1024/1440px，禁横向滚动，触控≥44px
  - 落地文件：`docs/requirements.md`(v3.0) + `docs/technical-design.md`(v3.0)，均重写
  - 用户意图："先落地文档"，尚未开始改代码；后续步骤：globals.css 统一令牌→简历页迁 /resume→首页 home/* 区块→Nav 路由互跳→响应式/主题联调

## 代码重构：多页架构落地（v3.0 → 代码）
- 用户指令："进入代码重构"，按 v3.0 文档实施
- globals.css：保留苹果蓝令牌（文档允许沿用 #3b5ccc），新增 `--font-display: "Space Grotesk"` 与 `.soft-card` 轻量卡片类 + `.font-display` 工具类
- layout.tsx：字体 link 增加 Space Grotesk + JetBrains Mono（与 Inter/Noto Sans SC 同链）
- use-scroll-spy.ts：改为可传 `sectionIds` 参数，适配首页/简历不同锚点
- Nav（route-aware）：`usePathname` 区分首页/简历；首页显示 关于/作品/技能/联系(锚点)+"简历"链接；简历页显示 关于/技能/项目/联系(锚点)+"首页"链接；均含互动版+主题切换；移动端抽屉同逻辑
- 简历页迁移：旧 `src/app/page.tsx` 整体迁至 `src/app/resume/page.tsx`，加独立 metadata；Bento 各 section 组件原样复用
- 首页门户：新建 `src/data/homepage.ts`（monogram/statusBadge/featuredProjectIds/quickLinks）+ `src/components/home/*` 六区块（hero/about/featured/skills/links/contact，Soft UI Evolution 风格）
- `src/app/page.tsx` 重写为首页门户，组装 home/* + Nav + Footer + BackToTop + 独立 metadata
- 新建 `src/app/sitemap.ts`：/ /resume /creative
- 构建：`env -u NODE_OPTIONS pnpm build` 零报错，3 路由静态生成；dev server localhost:3100 验证 / /resume /creative 均 200，内容拆分正确（首页无"经历"，简历无"精选作品"），无 hydration 错误

## 首页完全重构：Exaggerated Minimalism（夸张极简）
- 用户指令："首页需要完全重构"，选定 ui-ux-pro-max 数据库推荐的 Exaggerated Minimalism 路线（超大字号+巨幅留白+单色+单一蓝点缀，Best For: portfolios，浅深双模式）
- globals.css 新增 `.hm-*` 极简类：`.hm-section`(clamp 巨幅区块间距)、`.hm-hairline`(0.5px 发丝分隔)、`.hm-eyebrow`(小写字距蓝标签)、`.hm-name`(clamp(3.5rem,13vw,11rem) 展示字体 700/行高0.92/字距-0.045em)、`.hm-role`/`.hm-lead`/`.hm-h2`/`.hm-statement`、`.hm-btn-primary`/`.hm-btn-ghost`/`.hm-more`；复用现有令牌(--accent #3b5ccc、--font-display Space Grotesk)，深浅双主题自动适配
- 首页结构从 6 块(Soft UI) 改为 4 块：HomeHero(超大姓名+角色+tagline+双CTA) → HomeFeatured(精选作品网格, 复用 ProjectCard, sm:2列 lg:3列) → HomeAbout(理念陈述 homepage.philosophy + 现居/状态/专注 dl) → HomeContact(大 CTA: 复制邮箱+mailto+社交图标)
- homepage.ts：新增 `philosophy`(一句话理念) 与 `focus`(技能大类前2)，移除不再使用的 `quickLinks`
- 删除未使用组件 `src/components/home/home-skills.tsx` 与 `home-links.tsx`（技能/链接已在 /resume 完整呈现，首页留白更干净）
- 构建：`env -u NODE_OPTIONS pnpm build` 零报错；dev server localhost:3100 验证 / 返回 200 且含新极简标记、旧 Skills/Links 块已移除(0 匹配)、/resume 仍 200

## 导航栏新增"首页"图标 (13:47)
- 用户需求：进入其他页面时，导航栏应有首页图标，点击回到首页 `/`
- `src/components/layout/nav.tsx`：导入 `Home`(lucide)；桌面端导航改为 `justify-between`，左侧新增始终可见的 `Home` 图标 `Link href="/"`(glass 圆钮, `hidden md:inline-flex`)；移动端右上角按钮组加 `Home` 图标(`md:hidden`)。右侧跨页链接改为仅非简历页显示"简历"入口(`!isResume`)，避免与左侧首页图标在简历页重复
- `src/app/creative/page.tsx`：导入 `Home`，将右侧"标准版"链接替换为 `Home` 图标 + "首页"文案(同样指向 `/`)，保证互动版也有明确回首页入口
- 构建 `env -u NODE_OPTIONS pnpm build` 零报错；三页(/ /resume /creative)HTML 均含 `aria-label="返回首页"` 首页图标，dev server localhost:3100 已重启验证

## 首页去导航栏 + 模块化头像/社交 (14:33)
- 用户需求：首页不需要「关于/作品/技能/联系/简历/互动版」导航栏与左侧首页图标；首页需有头像区 + 社交媒体区(含github/微信公众号等)，二者做成可复用组件
- 新增模块化组件：
  - `src/components/ui/avatar.tsx`：图片优先、加载失败回退首字母 monogram（支持 link 回首页、size、priority）
  - `src/components/ui/social-links.tsx`：按 profile.social 动态渲染，含 WeChat 内联 SVG；支持 icon/chip 两种变体
  - `src/components/ui/theme-toggle.tsx`：从 Nav 抽出的明暗切换按钮，供首页复用
- `src/data/profile.ts`：social 增加 `wechat: "https://mp.weixin.qq.com/"`(类型已支持)
- `src/app/page.tsx`：移除共享 `<Nav/>`（首页导航与首页图标去掉）；改为极简顶栏仅 `<ThemeToggle/>`
- `src/components/home/home-hero.tsx`：改为两栏布局(左文右头像)，Hero 内含 `Avatar` + `SocialLinks` + CTA(查看简历→/resume, 互动版→/creative, 联系我→#contact)
- `src/components/layout/footer.tsx`：复用 `<SocialLinks/>`（自动含微信）
- /resume 仍保留完整 Nav（含首页图标回首页）；/creative 保留其自身导航
- 构建零报错；验证：首页无返回首页图标(0)，头像「张」回退渲染，社交5项齐全，/resume 仍有导航与首页图标

## 社交图标改为官方彩色（仅 GitHub + 微信）
- 需求：自媒体图标要彩色、跟官网一致；当前只显示 GitHub 和微信，其他后续再加
- `src/components/ui/social-links.tsx` 重写：新增 `BRAND` 品牌图标库（github 用官方 octocat 路径、color=var(--text-primary) 随主题反色浅黑/深白；wechat 用官方绿 #07C160）；只渲染已在 BRAND 登记图标的平台（缺字段或未完成图标自动跳过）。后续加 LinkedIn/掘金/知乎/Twitter 只需往 BRAND 追加一项
- 定位并修复旧 monochrome 图标来源：`home-contact.tsx`、`contact-section.tsx`（简历）、`creative/page.tsx` 各自内联渲染了 LinkedIn/掘金/知乎 的 lucide 图标 → 全部改为复用 `SocialLinks`（或创意页内联彩色 SVG），统一为仅 GitHub+微信 彩色
- `tsc --noEmit` 通过；三页(/ /resume /creative)验证：#07C160 与 github octocat 路径就位，LinkedIn/掘金/知乎 与 lucide 社交类均为 0
- 注：GitHub 官方标记为黑色，深色模式下用 currentColor 反白以保证可见；微信绿两主题均可见
